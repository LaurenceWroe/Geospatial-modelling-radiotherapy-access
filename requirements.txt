# Core geospatial / data
geopandas>=1.1.0
h3>=4.0.0          # H3 hexagonal grid (use v4 API)
shapely>=2.0.0
pyproj>=3.5.0
rasterio>=1.3.0
fiona>=1.9.0       # geopandas file I/O backend

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
xarray>=2023.1.0
openpyxl>=3.1.0    # reading .xlsx LINAC files

# Visualisation
matplotlib>=3.7.0
plotly>=5.18.0     # interactive charts (probability model tab)
pydeck>=0.8.0      # interactive H3 maps via Streamlit
streamlit>=1.32.0  # web app framework

# Utilities
requests>=2.28.0
pycountry>=22.3.5

# Optional (for netCDF4 / GLOBOCAN tensor loading)
# netCDF4>=1.6.0   # if xarray cannot read .nc without it
